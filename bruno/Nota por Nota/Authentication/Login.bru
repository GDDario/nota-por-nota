meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{url}}/login
  body: json
  auth: none
}

body:json {
  {
    "email": "jhon@doe.com"
  }
}

script:post-response {
  bru.setEnvVar('access_token', res.body.data?.access_token);
}
