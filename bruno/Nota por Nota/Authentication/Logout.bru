meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{url}}/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "email": "jhon@doe.com",
    "password": "password"
  }
}
